# encoding: utf-8
# 僅使用「五笔·拼音」（wubi_pinyin）
# 复制到：~/.config/ibus/rime/default.custom.yaml

patch:
  schema_list:
    - schema: wubi_pinyin
  ascii_composer:
    good_old_caps_lock: true
    switch_key:
      Shift_L: commit_code
      Shift_R: commit_code
