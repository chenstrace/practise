# encoding: utf-8
# 四码顶字：须用「键/路径」逐项 patch，勿写整段 translator/speller，否则会覆盖坏配置。
# 参考：https://github.com/rime/librime/issues/289
# 复制到：~/.config/ibus/rime/wubi_pinyin.custom.yaml

patch:
  speller/max_code_length: 4
  speller/auto_select: true
  translator/enable_sentence: false
